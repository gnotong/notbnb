{% extends 'base.html.twig' %}

{% block title %}Booking!{% endblock %}

{% block body %}
    <div class="container">
        <h1>You are booking for the ad: {{ ad.title }}</h1>

        <p>You are about to book the property of <strong>{{ ad.author }}</strong>, we will handle the rest</p>

        {{ form_start(form) }}
            <div class="alert alert-light p-2">
                <h2>Start by telling us when will you arrive</h2>
                <div class="row">
                    <div class="col">
                        {{ form_row(form.startDate) }}
                    </div>
                    <div class="col">
                        {{ form_row(form.endDate) }}
                    </div>
                </div>
            </div>

        <h4>Cost of stay : &dollar;<span id="amount">...</span></h4>
        <span id="days">0</span> night(s) at {{ ad.price }} / night

        <h2>You have a comment ?</h2>
        {{ form_row(form.comment) }}

        <h2>You have finish !</h2>
        <p>Holidays of your dreams ? Accommodation for business ? Don't worry, we are always available !</p>

        <button type="submit" class="btn btn-success">I book !</button>

        {{ form_end(form) }}
    </div>
{% endblock %}
