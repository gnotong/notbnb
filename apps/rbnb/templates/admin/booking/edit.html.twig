{% extends 'admin/base.html.twig' %}

{% block title %}Booking edit{% endblock %}

{% set ad = booking.ad %}

{% block body %}
    <div class="container">
        <h1 class="my-1">You are changing the booking for the ad: <em>{{ ad.title }}</em></h1>

        {{ form_start(form) }}

        <div class="row">
            <div class="col">
                {{ form_row(form.startDate) }}
                {{ form_row(form.endDate) }}
                {{ form_row(form.comment) }}
            </div>
            <div class="col">
                {{ form_row(form.booker) }}
                {{ form_row(form.ad) }}
                <button type="submit" class="btn btn-success">
                    <i class="fas fa-check"></i>
                    Update !
                </button>
            </div>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}
